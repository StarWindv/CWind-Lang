// SA false positive: generic struct field assignment is not type-checked
// against the substituted field type.  Expected: no errors.  Reported:
//   cannot assign String to T

struct Box<T> {
    pub x: T,
}

fn f(b: Box<String>) -> None {
    b.x = "y";
}
