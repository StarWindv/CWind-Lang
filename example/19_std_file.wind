// 此用例待扩展

use std::baseimpl::file::File;
use std::baseimpl::file::FileMode;

fn main() {
    let mut f: File = File::open("a.txt", FileMode::Write).unwrap();
    f.write_line("Hello, CWind File!");
    f.close();
}
