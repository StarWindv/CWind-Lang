use std::ctypedef::*;
#[cfg(target_os = "windows")]
extern "system" {
    // <processthreadsapi.h>
    #[link_name="GetCurrentThreadID"]
    fn gettid() -> c_uint;
}

#[cfg(not(windows))]
extern "system" {
    fn gettid() -> c_uint;
}
