// 别管这里, 我正在加东西
use std::ctypedef::*;
#[cfg(target_os = "windows")]
extern "system" {
    // <processthreadsapi.h>
    #[link_name="GetCurrentThreadID"]
    fn gettid() -> c_uint;
}

#[cfg(not(windows))]
extern "system" {
    fn gettid() -> c_uint;
}
