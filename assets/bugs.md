## 1) 标记

| 图标 | 含义                        |
|------|-----------------------------|
| ⬜   | 未修复                      |
| ✅   | 已修复                      |
| ⚠   | 暂缓修复                    |

## 2) 事项

部分用例可在`bugs`中找到, 打勾的即为已修复

| 状态 | 序号 | Bug 描述                                                                                                                                                   | 备注                                                                                                                                       |
|------|------|------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------|
| ✅   | 1    | 修复泛型方法传入的 self 所绑定的类型错误的问题                                                                                                             |                                                                                                                                            |
| ✅   | 2    | 修复`for`遍历容器时类型校验错误的 Bug ( 例如`for kv in map.entry()`会被`cwindc`拒绝)                                                                       |                                                                                                                                            |
| ✅   | 3    | 修复 `impl<T: BoundTrait<Another>> for Type<T>` 中不检查是否存在 `BoundTrait` 的 Bug                                                                       |                                                                                                                                            |
| ✅   | 4    | 修复顶层变量声明失败的问题 ( 如 `const Data: Map<A, B> = { xxxx: xxxx }` 在 `cwindc` 中会被标识为 `undeclared variable` )                                  |                                                                                                                                            |
| ✅   | 5    | 类型检查不穿透容器                                                                                                                                         |                                                                                                                                            |
| ✅   | 6    | 不支持 `!>` 和 `!<` 的问题 (Lexer 映射到 `LE` 和 `GE` 即可, 不需要额外 Token)                                                                              |                                                                                                                                            |
| ✅   | 7    | 编译期未验证 group 精化类型                                                                                                                                |                                                                                                                                            |
| ✅   | 8    | 实现`trait`时, 需要返回`Self`的函数未能将`Self`绑定到自身名称上 (例如在结构体`MyStruct`的方法`fn method(...) -> Self`中, 未能将`Self`绑定到`MyStruct`)     |                                                                                                                                            |
| ✅   | 9    | 实现无泛型参数的内置`trait`和部分其它内置`trait`时, 完全不检查是否真的实现了某函数方法                                                                     |                                                                                                                                            |
| ✅   | 10   | 如果手动同时实现了`From<A> for B`和`Into<B>`中的`into`方法, 没有检查是否出现了重复的`into`                                                                 |                                                                                                                                            |
| ✅   | 11   | 未检查实现内置`trait`对应方法时的返回值与参数                                                                                                              |                                                                                                                                            |
| ✅   | 12   | 编译器前端未验证某对象在`builtins::print`时是否具有`Display::to_string`方法                                                                                |                                                                                                                                            |
| ✅   | 13   | 编译器前端未在对象被打印时生成对应的`Display::to_string`方法调用                                                                                           |                                                                                                                                            |
| ✅   | 14   | 编译器后端未使用对象的`Display::to_string`方法                                                                                                             |                                                                                                                                            |
| ✅   | 15   | 函数传参时未移动所有权 (赦免 self, 其在现阶段需要传递引用) (我们不应该搞值拷贝那一套)                                                                      |                                                                                                                                            |
| ✅   | 16   | 编译器前端未验证`group@struct -> { field }`中的`field`是否符合`group`所接收的类型                                                                          |                                                                                                                                            |
| ✅   | 17   | 编译器后端未验证`String.format()`方法所接收的参数数量是否匹配花括号数量                                                                                    |                                                                                                                                            |
| ✅   | 18   | `Parser`未能正确验证空`for-in`循环后花括号的开闭                                                                                                           |                                                                                                                                            |
| ✅   | 19   | `From` 应该是`Into`的关联`trait`, 而不应该要求手动实现`into`(在`impl From<A> for B`时, 未自动实现`A.into`)                                                 |                                                                                                                                            |
| ✅   | 20   | 所有权系统验证完毕后错误的将引用等符号传递给后端                                                                                                           |                                                                                                                                            |
| ✅   | 21   | SA 未能正确验证泛型与 Into 的绑定关系                                                                                                                      |                                                                                                                                            |
| ✅   | 22   | 修复 `elif` (if-chains) 代码生成失败的问题 (后端读取 `elif.then` 而前端序列化为 `ElifBranch.body`, 且最后一个 elif 的 false 边复用 `else_bb` 导致死循环)   |                                                                                                                                            |
| ✅   | 23   | 修复 `typedef` 别名与 `type-where` 精化类型变量在后端被当作未知类型的问题 (如 `print` 输出 `?`); 现优先采用 SA 解析后的 `ann.type` (`Self` 仍走实例绑定)   |                                                                                                                                            |
| ✅   | 24   | SA 在 main 函数返回值为非`int`/非None/空返回值时未能报错                                                                                                   | 允许整数类型 (含 Byte)/None/省略/never `!`; 空返回值场景由既有通用检查兜底                                                                 |
| ⚠   | 25   | `String.format`函数会将过长数值自动截断到(整数位数+小数位数=6位)                                                                                           | 包导入系统实现后会脱离 C 底层以重做                                                                                                        |
| ⚠   | 26   | `String.format`函数在格式化自定义结构体对象时后端静默崩溃                                                                                                  | 包导入系统实现后会脱离 C 底层以重做                                                                                                        |
| ⚠   | 27   | `String.format`函数在格式化自定义结构体对象时前端没有调用对应的`Display::to_string`                                                                        | 包导入系统实现后会脱离 C 底层以重做                                                                                                        |
| ⚠   | 28   | SA 没有验证待`format`对象是否实现了`Display`                                                                                                               | 包导入系统实现后会脱离 C 底层以重做                                                                                                        |
| ✅   | 29   | SA 没有正确将`std::prelude`里定义的类型别名解析到FFI边界                                                                                                   | 别名在 C-ABI 校验前统一展开 (含字段/数组元素/指针被指类型)                                                                                 |
| ✅   | 30   | `main` 函数不能正确接收程序参数                                                                                                                            | C 入口改带 `(argc, argv)`, rt 打包成 `Vector<String>` 注入 main                                                                            |
| ✅   | 31   | SA 没有在用户使用一个内置类型已实现的内置trait为原类型实现时报错(重复实现)(无论有没有孤儿规则都该修)                                                       | 包导入系统实现后会脱离 C 底层以重做                                                                                                        |
| ✅   | 32   | 通配导入的依赖闭包不能达到其他模块的 pub extern 块 (通配导入末段是星号, 导出面也跳过无名块); 且 prelude 重导出被显式导入的闭包项误屏蔽                     | 依赖闭包按声明名索引 + pub extern 块进编译面 + 屏蔽仅限入口/包 lib 层                                                                      |
| ✅   | 33   | 数值 `as` `number type` 时, 虽然已计算出`别名`就是某数值类型, 但未能成功进行`as`操作                                                                       | cast 目标/操作数都展开别名 (`u32`->`UInt32`), 结果与注解写展开后类型, 后端按规范名转换                                                     |
| ✅   | 34   | 在 extra 块内使用无 Return 的尾返回时, 不能成功将自身结构体类型和 Self 进行绑定                                                                            | 方法体内 let 声明/非 self 形参的 `Self` 一律绑定到所属类型, 与返回类型同路径                                                               |
| ✅   | 35   | 当结构体属性存在定长内联数组时, 在其实现 extra 时会出现奇怪的解析错误                                                                                      | 新增 `[x; N]` 重复字面量 (parser/SA/后端), `_brace_is_struct_construct` 忽略括号内 `;`, 数组元素别名展开                                   |
| ✅   | 36   | 奇怪的`Parameter requires a type annotation`错误                                                                                                           | 导入模块的 parse 错误带 `source` 归属到模块文件, CLI 按各自文件渲染, 不再锚定入口文件                                                      |
| ✅   | 37   | 同文件内声明的 cffi 函数错误的要求 pub 可见性调整, 且模块归属错误                                                                                          | extern 块成员进声明文件裸名可见集; std prelude 导出面对所有模块文件可见 (Rust 语义)                                                        |
| ✅   | 38   | 不能给 CFFI 函数标记发散                                                                                                                                   |                                                                                                                                            |
| ✅   | 39   | SA 没有检查泛型坍缩后的类型与传入值是否匹配                                                                                                                |                                                                                                                                            |
| ✅   | 40   | SA 没有检查泛型函数是否存在                                                                                                                                |                                                                                                                                            |
| ✅   | 41   | Parser 错误的不允许`extern`块中出现`pub fn name`                                                                                                           | 成员可见性 = 块级 pub 与成员 pub 取或, 并进导出面                                                                                          |
| ✅   | 42   | Parser 未能正确解析带模块限定名的泛型 trait 实现                                                                                                           | 限定路径类型支持携带泛型实参, SA 经 use 别名表把 `mod::Trait` 规范化为扁平名 (同 todo-81 模式)                                             |
| ✅   | 43   | SA 未在 trait 实现前展开类型别名, 且莫名出现更多条错误信息                                                                                                 | impl/extra 目标在一致性校验前展开别名 (含重键), `_auto_shadow_names` 不再把 impl/extra 当本地声明遮蔽 prelude                              |
| ✅   | 44   | SA 未在 用户调用结构体对象的非 `pub` 字段时报错                                                                                                            |                                                                                                                                            |                                                                                                                |
| ✅   | 45   | 在 `&self` 接收者的方法里调用「其它类型无接收者的关联函数」之后, 再读取 `self` 的任意字段, 会误报 `Value 'self' is used after move`                        | `_method_self_is_ref` 对**无 self 参数**的关联函数返回 False, 被两处 move 标记当成按值 self                                                |                                                                                                                |
| ✅   | 46   | 函数调用时 Parser 不允许使用`&mut arg`                                                                                                                     | 类型位 `&mut T` 与借用位 `&mut expr`; 可变性语义随 Rust; 标量写穿见 todo-145, 复现文件剩余报错属 todo-75/95                                |
| ✅   | 47   | 泛型容器使用字面量初始化时, SA 未能主动绑定正确的数值类型(哪怕使用了`value as type`)                                                                       | Vector/Map 字面量元素绑定注解类型, 逐元素校验; 后端装箱宽度问题一并消除                                                                    |
| ✅   | 48   | bug-43 回归, 泛型容器内的类型别名未在分析前展开                                                                                                            | 别名展开递归进泛型实参与指针被指类型; 精化别名在实参位保留原名, 兼容性比较时才展开                                                         |
| ✅   | 49   | `extra` 上的泛型参数未传播到`extra`内函数                                                                                                                  | `extra Cell<T>` 归一化为 `extra<T> Cell<T>`; 裸名构造按 owner/期望类型制导; 具体特化形式见 todo-147                                        |
| ✅   | 50   | SA 未检查接收`&mut self`的函数接收到的参数究竟是不是`mut`的                                                                                                |                                                                                                                                            |
| ✅   | 51   | SA 不能区分泛型T和结构体T                                                                                                                                  |                                                                                                                                            |
| ✅   | 52   | SA 没有在进行检查前展开所有别名, 导致实现trait时`impl`中的原始名不等于`trait`中的别名                                                                      |                                                                                                                                            |
| ✅   | 53   | 后端对变量作用域的解析不正确                                                                                                                               | 已修                                                                                                                                       |
| ✅   | 54   | prelude 自动导入与本地同名顶层函数发生签名杂交 (本地 `panic(String)` + prelude `std::panic::panic(&String)` 时, 调用按本地解析、实参却按 prelude 形参校验) | 已修                                                                                                                                       |
| ✅   | 55   | 分析 std::baseimpl::random::unsafe_rand 的崩溃原因                                                                                                         | 已定位并修复                                                                                                                               |
| ✅   | 56   | 导入 trait 后不能正确解析其对应实现                                                                                                                        | impl/extra 块名进模块 pub_names, owner 不在导出面也被放行                                                                                  |
| ✅   | 57   | SA 不能正确导入 `pub const` 常量                                                                                                                           | `mod::CONST` 在成员/调用两条路径解析为 const 绑定, 后端复用顶层 const 全局槽                                                               |
| ✅   | 58   | SA 不能正确分析带`*mut`参数的函数指针的传递                                                                                                                | `_expand_type` 递归进 fn 签名段, 裸名实参按展开后签名逐段严格比对                                                                          |
| ✅   | 59   | 后端不允许函数指针传递给 FFI 函数                                                                                                                          | 回调签名段内原始指针按 C 指针映射, 适配器/签名构造全链放行                                                                                 |
| ✅   | 60   | cwindf 不能正确挡住数值类型上限 + 1                                                                                                                        | BinOp 折叠值按结果类型范围检查 (`_check_expr_range`), `_fold_expr` 变量携带折叠; 裸字面量运算与运行期未知值豁免                            |
| ✅   | 61   | bug-56 回归 (见测试集) 修复见 todo-158                                                                                                                     | per-root trait impl registry, entry 级把导入 trait 的 impl 块自动拉进编译面 (rustc 语义, 不依赖 prelude re-export); todo-158 可修复此问题  |
| ✅   | 62   | SA 居然不允许 `impl Traits for Alias`                                                                                                                      | `_expand_impl_target_aliases` 改为结构展开 + `_require_type_target`/`_check_type` 识别别名和 `_cwind_builtins`                             |
| ✅   | 63   | SA 居然压根没有解析`mod name { ... }`是否真实存在                                                                                                          | 通配导入尾段必须完整解析 (`nums::*` 类幻影尾段报错), 外联 `mod name;` 声明悬空时在声明处报错                                               |
| ✅   | 64   | SA 对 mutable 的解析不正确                                                                                                                                 | `&T`/`&mut T` 形参按 Rust 自动借用语义传递引用, 不移动实参 (调用点 `_check_user_call` 按 `param.type.ref` 跳过 move 标记)                  |
| ✅   | 65   | SA 居然不允许泛类型和泛 trait 的 impl, 而且方法解析也不正确                                                                                                | impl 目标识别泛型形参 (`_require_type_target`), 新增泛型 trait 约束表 (`generic_trait_bounds`) 支持 `U::from(x)` 按约束 trait 的方法表解析 |
| ✅   | 66   | SA 居然不允许对 *mut/*const T 来实现 trait                                                                                                                 | `_require_type_target` 放行 `*const T`/`*mut T` 扁平名作为 impl 目标 (Rust 允许对裸指针实现 auto trait)                                    |
| ✅   | 67   | `macro_rules!` 对部分符号的解析不正确                                                                                                                      | 片段起始集对齐语法: expr 补 `*`/`&`/`+` 一元前缀, type 补 `!` (never)                                                                      |
| ✅   | 68   | 在提供默认实现的 trait 中无法正确绑定`Self`                                                                                                                | 默认体 Self 绑定到 trait (含 supertrait 方法表); impl 无需重复提供默认方法; 继承默认的调用点分派待 todo-194                                |
| ✅   | 69   | 前端 test-79 意外失败                                                                                                                                      | bootstrap 兜底面只放行自身注册的内建名, std 模块函数/导入物化回归 todo-79 门控                                                             |
| ✅   | 70   | 单字母命名且未声明泛型的结构体在类型位被误判为泛型形参 (51回归)                                                                                            | 泛型作用域退出改为快照恢复 (`_enter/_leave_defined`), 只回收本作用域新登记的名字; std `Wrapping<T,S>` 等形参不再误删同名用户类型           |
| ✅   | 71   | match 对布尔的穷尽性检查不正确                                                                                                                             |                                                                                                                                            |
| 🚫   | 72   | ~~字符串的栈上复制结果不正确 ( clone 居然返回相同地址?! )~~                                                                                                | clone 实现错误, 不是后端错误                                                                                                               |
| ✅   | 73   | IF 控制流出现不应该出现的`used after move`                                                                                                                 |                                                                                                                                            |
| ✅   | 74   | 语句位(丢弃位置)的 match/if 臂携带值未报错                                                                                                                 | SA `_check_match` 语句位路径补丢弃检查                                                                                                     |
| ✅   | 75   | 项目自带 libs 覆盖 std 根时, 内建绑定指向 bootstrap 兜底副本节点 (不在节点池), 序列化过滤后后端报 `method binding not found`                               |                                                                                                                                            |
| ✅   | 76   | Map 下标 key 类型不校验, `m[0]` 以 Int 索引 `Map<String,_>` 放行到运行时静默查空                                                                           |                                                                                                                                            |
| ✅   | 77   | ~~作用域语义错误流入后端: 名称解析/遮蔽本应纯前端, 后端却按名字维护扁平变量表重算~~ 似乎是误报                                                             | ~~SA 已标注 ann.binding (ref 即局部编号), 所以完全可以在前端把作用域吞掉; 后端 scope push/pop 零调用; 显式 locals 后后端只按编号取槽~~     |
| ⚠   | 78   | 导入模块 (非 auto prelude 层) 的函数体在扁平命名空间内按裸名重解析, 入口同名声明会劫持其内部调用 (如 fn print 劫持 stream_to_stdout 内的 print)            | 过程宏程序已用内部名规避; 根因属 bug-54 同类, 待遮蔽改革                                                                                   |
| ✅   | 79   | 降糖后没有正确检查所有权与借用关系                                                                                                                         | 借用位不可被按值 `self` 方法消耗 (Rust E0507); for-in 降糖的 `into_iter` 一视同仁                                                          |
| ✅   | 80   | 项目模块与main文件里同时定义了两个重名未导出宏时, main文件里不能正确选择自己定义的宏, use xxx::* 时不能正确的将未导出宏排除, 导致main选择了错误的模块宏    |                                                                                                                                            |
| ✅   | 81   | 后端对内置类型的 new 方法处理不正确                                                                                                                        |                                                                                                                                            |
| ✅   | 82   | 在极大量堆分配下(500万次)出现异常长的卡顿与等待时间                                                                                                        | 触发阈值默认随存活堆自适应, 显式设 step 时固定); arena 段链表 O(1) 追加; OS 归还候选 UAF 一并修复                                          |
| ✅   | 83   | 前端`不在乎`一个内置类型是否真的存在                                                                                                                       | 去除不知道什么时候留下的特权代码                                                                                                           |
| ✅   | 84   | 反向 FFI: C 视图恰为 8B 的载荷/无载荷枚举, 按值传参与返回在 Win64 错走 byval 指针/sret, 调用即崩 (12B 枚举与 ≤8B 结构体正常)                               | SysV 待 Linux CI                                                                                                                           |
| ⬜   | 85   | 无法正确识别文件内的`mod`块重导出                                                                                                                          |                                                                                                                                            |


- 38\~41 号 bug 编号曾不对, 现已更正 (先前为 37~40)
