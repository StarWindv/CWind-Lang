// bug-33: 数值 `as` 的目标类型是 prelude 类型别名 (u32/i8/f64) 时,
// SA 必须展开别名后放行, 后端按展开后的底层类型做宽度转换。
// 输出 0;-1;3;7 (每行一个), 退出码 0

fn main() -> Int {
    print(0 as u32);
    print(255 as i8);
    print(3.9 as Int);
    print(7 as f64);
    return 0;
}
