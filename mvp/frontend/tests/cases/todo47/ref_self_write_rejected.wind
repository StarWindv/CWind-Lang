struct S { x: Int }extra S { fn bump(&self) { self.x = 5; } }fn main() { let s: S = S { 0 }; print(s.bump()); }
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
