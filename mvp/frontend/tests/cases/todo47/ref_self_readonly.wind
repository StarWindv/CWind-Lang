struct S { x: Int }extra S { fn get(&self) -> Int { return self.x; } }fn main() { let s: S = S { 7 }; print(s.get()); }
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
