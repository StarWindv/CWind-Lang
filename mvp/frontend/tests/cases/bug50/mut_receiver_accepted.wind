// bug-50 正例: mut 绑定可以调用 &mut self 方法 (含链式两次调用)
struct Counter { n: Int }

extra Counter {
    pub fn bump(&mut self) -> Int {
        self.n += 1;
        return self.n;
    }
}

fn main() -> Int {
    let mut c: Counter = Counter { 0 };
    c.bump();
    return c.bump();
}
