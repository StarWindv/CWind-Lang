// bug-50: 非 mut 形参同样是不可变接收者
struct Counter { n: Int }

extra Counter {
    pub fn bump(&mut self) -> Int {
        self.n += 1;
        return self.n;
    }
}

fn consume(c: Counter) -> Int {
    return c.bump();
}

fn main() -> Int {
    let c: Counter = Counter { 5 };
    return consume(c);
}
