// extra 省略前导泛型实参: 具体类型实参自 todo-147 起合法
// (Rust `impl Cell<i32>` 特化), 由 cases/todo147 承接语义锁定;
// 此处保留一个最小 clean 形 (含 &self 接收者) 防回归。
struct Cell<T> { value: T }

extra Cell<Int> {
    pub fn get(&self) -> Int { return self.value; }
}
