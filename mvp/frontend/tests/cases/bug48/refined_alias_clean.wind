type Test1 = Int8 where { self > 0; self < 100; }

fn main() {
    let v: Vector<Test1> = [50];
    print(v.length());
}
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
