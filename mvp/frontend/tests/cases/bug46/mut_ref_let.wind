// &mut T 类型的 let 绑定同样写穿
struct C { n: Int }

fn main() {
    let mut c: C = C { 1 };
    let r: &mut C = &mut c;
    r.n = 5;
    print(r.n);
    print(c.n);
}
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
