fn main() {
    let a: [Int32; 4] = [7; 4];
    let b: [Byte; 3] = [0 as Byte; 3];
    print(a[3]);
    print(b[2]);
}
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
