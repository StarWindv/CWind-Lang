// todo-65: 混合宽度平铺标量聚合现可通过 SA (12 字节以内按
// 目标 ABI 分派: Win64 8 字节内单寄存器镜像 / SysV 寄存器对;
// {Int8, Int32} 自然布局大小为 8 字节)
extern "C" {
    fn f(m: Mixed) -> Int32;
}
struct Mixed {
    a: Int8,
    b: Int32,
}
fn main() -> Int { return 0; }
