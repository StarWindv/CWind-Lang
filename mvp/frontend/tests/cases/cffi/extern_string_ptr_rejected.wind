// todo-182: *const String 放行 —— 指针位任意非泛型被指类型按不透明
// 地址直传 (String 句柄地址即字节指针; C 侧声明 char** 或句柄即可)。
#[link(name = "cwindmath", kind = "static", path = "./libcwindmath.a")]
extern "C" {
    fn f(s: *const String) -> Int32;
}
fn main() -> Int { return 0; }
