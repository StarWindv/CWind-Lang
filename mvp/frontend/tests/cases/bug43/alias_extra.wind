typedef i32 = Int32;

extra i32 {
    pub fn zero() -> i32 {
        return 0;
    }
}

fn main() {
    print(i32::zero());
}
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
