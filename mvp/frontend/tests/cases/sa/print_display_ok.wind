struct User {}impl ToString for User { fn to_string(&self) -> String { return "UserString"; }}fn main() { let u: User = User {}; print(u); }
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
