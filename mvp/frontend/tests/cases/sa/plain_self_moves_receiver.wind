struct S { x: Int }extra S { fn take(self) -> Int { return self.x; } }fn main() { let s: S = S { 1 }; print(s.take()); print(s.take());}
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
