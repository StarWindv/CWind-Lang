// 绑定后按注解元素类型做范围检查
typedef i8_ = Int8;

fn main() {
    let v: Vector<i8_> = [1000];
    print(v.length());
}
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
