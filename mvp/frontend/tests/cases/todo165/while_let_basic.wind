enum Opt { Some(Int), None }

fn sum_desc(o: Opt) -> Int {
    let mut o2: Opt = o;
    let mut sum: Int = 0;
    while let Opt::Some(x) = o2 {
        sum += x;
        o2 = Opt::None;
    }
    return sum;
}
