extra Config {
    const LIMIT: Int32 = 1;
}

struct Config { pub n: Int32 }

fn main() -> Int {
    print(Config::NOPE);
    return 0;
}
extern "CWind" {
    fn baseprint(value: String);
}

fn print<T: ToString>(value: &T) {
    baseprint(value.to_string());
}
