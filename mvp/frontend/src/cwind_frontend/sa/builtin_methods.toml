# Built-in traits, methods and module functions for semantic analysis.
#
# Adding a trait or a method is a data edit here; no Python code changes needed.
#
# Type vocabulary for `args` / `returns`:
#   concrete types : Int Int8 Int16 Int32 Int64 UInt UInt8 UInt16 UInt32 UInt64
#                    Float Float64 String Bool Byte
#                    None Tuple Vector Map Set Iterator
#   Self            the receiver's type
#   SameAsGeneric   shorthand for SameAsGeneric:1
#   SameAsGeneric:N the receiver's Nth generic argument, 1-based
#                   (Vector<T> / Set<T> -> :1; Map<K, V> -> :1 = K, :2 = V)
#   TraitArg:N      the Nth type argument of a directional trait
#                   instantiation, 1-based.  A type opts into such a trait
#                   with an angle-bracketed entry in `traits`:
#                     [types.UInt]
#                     traits = ["From<String>"]   # UInt::from(String) -> UInt
#                     [types.String]
#                     traits = ["Into<UInt>"]     # String.into() -> UInt
#                   TraitArg:1 then resolves to String / UInt respectively.
#   SameTypeOther   the same type as the receiver
#   AnyInt          any integer type (Int / Int8 / Int16 / Int32 / Int64 /
#                   UInt / UInt8 / UInt16 / UInt32 / UInt64)
#   EveryNumber     any numeric type (all of the above + Float / Float64 / Byte)
#   AnyGeneric      any generic instantiation (Vector<...>, Map<..., ...>, Set<...>,
#                   or a user generic type with arguments)
#   Whatever        anything (argument only; unchecked)
#   Any             unknown/unspecified (return only)
#   !               never type: a diverging call (e.g. `exit`) coerces to
#                   any type, and a `-> !` function must diverge on all paths
#   Context         into() only: the target type comes from the call-site
#                   expected type (e.g. `let n: UInt = s.into();`)
#
# Methods that need a receiver declare an explicit leading ``Self`` argument;
# static methods / module functions omit it.  The loader uses that to tell
# instance methods apart from static ones and strips ``Self`` when checking
# call arity and argument types.
#
# `args` entries may carry a count prefix:
#   "Type"       exactly one argument of Type
#   "N: Type"    exactly N consecutive arguments of Type
#   "*: Type"    any number of arguments of Type (only allowed as the last entry)
# So a static container constructor can be declared as
# `new = { args = ["*: SameAsGeneric:1"], returns = "Self" }` without a
# separate `variadic` flag (that flag said nothing about the types of the
# extra args).

# Built-in objects are named values that need no declaration, e.g. `None`.
[objects]
[objects.None]
type = "None"

[traits]
Display   = ["to_string"]
Sortable  = ["sort"]
Iterable  = ["iter"]
Sliceable = ["slice"]
Add       = ["add"]
Format    = ["format"]
From      = ["from"]
Into      = ["into"]


[trait_methods]
to_string = { args = ["Self"], returns = "String" }
sort      = { args = ["Self"], returns = "None" }
iter      = { args = ["Self"], returns = "Iterator" }
slice     = { args = ["Self"], returns = "SameTypeOther" }
add       = { args = ["Self", "SameTypeOther"], returns = "SameTypeOther" }
format    = { args = ["Self", "*: Whatever"], returns = "String" }
# Rust-shaped directional traits:
#   fn from(value: TraitArg:1) -> Self   (static, attached to the target)
#   fn into(self) -> TraitArg:1          (instance, attached to the source)
# A type opts in with `traits = ["From<X>"]` / `traits = ["Into<X>"]`;
# user conversions via ``impl From<X> for Y`` remain source-declared.
from      = { args = ["TraitArg:1"], returns = "Self" }
into      = { args = ["Self"], returns = "TraitArg:1" }

[types.Vector]
traits = ["Display", "Iterable", "Sliceable", "Sortable", "Add"]
generic = "element"

[types.Vector.methods]
new         = { args = [ ], returns = "Self" } # The "new" method with values is temporarily not allowed.
length      = { args = ["Self"], returns = "UInt" }
contains    = { args = ["Self", "SameAsGeneric:1"], returns = "Bool" }
push_back   = { args = ["Self", "SameAsGeneric:1"], returns = "None" }
pop_back    = { args = ["Self"], returns = "SameAsGeneric:1" }
extend_with = { args = ["Self", "SameTypeOther"], returns = "None" }
insert_at   = { args = ["Self", "AnyInt", "SameAsGeneric:1"], returns = "None" }
index_of    = { args = ["Self", "SameAsGeneric:1"], returns = "UInt" }
get         = { args = ["Self", "AnyInt"], returns = "SameAsGeneric:1" }
set         = { args = ["Self", "AnyInt", "SameAsGeneric:1"], returns = "None" }
remove_at   = { args = ["Self", "AnyInt"], returns = "None" }
clear       = { args = ["Self"], returns = "None" }

[types.String]
traits = ["Display", "Iterable", "Sliceable", "Add", "Format"]

[types.String.methods]
new      = { args = [], returns = "Self" }
length   = { args = ["Self"], returns = "UInt" }
matches  = { args = ["Self", "String"], returns = "Bool" }
contains = { args = ["Self", "String"], returns = "Bool" }
# 方向性转换: 数值类型经 ``From<String>`` 提供 ``T::from(s)``;
# ``s.into()`` 的目标类型由调用处推断 (Context), 例如
# `let n: UInt = s.into();` 解析为 UInt。
into     = { args = ["Self"], returns = "Context" }

[types.Map]
traits = ["Display", "Iterable"]
generic = "value"

[types.Map.methods]
new      = { args = [], returns = "Self" }
length   = { args = ["Self"], returns = "UInt" }
contains = { args = ["Self", "SameAsGeneric:1"], returns = "Bool" }
entry    = { args = ["Self"], returns = "Tuple<SameAsGeneric:1, SameAsGeneric:2>" }
#get_last = { args = ["Self"], returns = "Tuple<SameAsGeneric:1, SameAsGeneric:2>" }
get      = { args = ["Self", "SameAsGeneric:1"], returns = "SameAsGeneric:2" }
set      = { args = ["Self", "SameAsGeneric:1", "SameAsGeneric:2"], returns = "None" }
remove   = { args = ["Self", "SameAsGeneric:1"], returns = "None" }
clear    = { args = ["Self"], returns = "None" }

[types.Set]
traits = ["Display", "Iterable"]
generic = "element"

[types.Set.methods]
new      = { args = [ ], returns = "Self" }
length   = { args = ["Self"], returns = "UInt" }
contains = { args = ["Self", "SameAsGeneric:1"], returns = "Bool" }
add      = { args = ["Self", "SameAsGeneric:1"], returns = "None" }
remove   = { args = ["Self", "SameAsGeneric:1"], returns = "None" }
clear    = { args = ["Self"], returns = "None" }

[types.Tuple]
traits = ["Display", "Sliceable"]

[types.Tuple.methods]
length = { args = ["Self"], returns = "UInt" }


[types.Int]
traits = ["Display", "From<String>", "Into<String>"]

[types.Int8]
traits = ["Display", "From<String>", "Into<String>"]

[types.UInt]
traits = ["Display", "From<String>", "Into<String>"]

[types.UInt8]
traits = ["Display", "From<String>", "Into<String>"]

[types.Int16]
traits = ["Display", "From<String>", "Into<String>"]

[types.UInt16]
traits = ["Display", "From<String>", "Into<String>"]

[types.Float]
traits = ["Display", "From<String>", "Into<String>"]

[types.Float64]
traits = ["Display", "From<String>", "Into<String>"]

[types.Byte]
traits = ["Display", "From<String>", "Into<String>"]

[types.Bool]
traits = ["Display"]

[types.Int32]
traits = ["Display", "From<String>", "Into<String>"]

[types.UInt32]
traits = ["Display", "From<String>", "Into<String>"]

[types.Int64]
traits = ["Display", "From<String>", "Into<String>"]

[types.UInt64]
traits = ["Display", "From<String>", "Into<String>"]

[modules.builtins]
print    = { args = ["Whatever"], returns = "None", is_pure = false }
readline = { args = [], returns = "String", is_pure = false }
type_of  = { args = ["Whatever"], returns = "Any", is_pure = true }

# exit 是 never 类型: 调用点之后不会继续执行, 可用作 `-> !` 函数的发散尾。
exit    = { args = ["AnyInt"], returns = "!", is_pure = false }
