trait Temp {
    fn t(&self) -> Self;
}

impl<T> Temp for Vec<T> {
    fn t(&self) -> Self {
        Self::new() // 很诡异, 这里如果写的是`Vec::new()`就没事了
    }
}

fn main() {
    let t: Vec<u8> = Vec::new();
    t.t();
}
